# set_model_parameters_v08.r

WEEK0 <- 0; 
HDR <- 0; 
U_HDR2 <- 1;   # use extra paramaters for host-donor rates of activated and CD69+ states
BRF <- 1.1   # birth rate FACTOR - multiple of total birth rates (e.g. 100000 for Treg) in .csv      
MAX_FLOW_TO_N3_FACTOR <- 10 
EPS <- 9.920635e-05 * 2   # 2 minutes


